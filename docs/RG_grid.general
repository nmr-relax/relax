file = RG_grid
grid = 20 x 20 x 20
format = ascii
interleaving = field
majority = row
field = data
structure = scalar
type = float
dependency = positions
positions = regular, regular, regular, 0, 1, 0, 1, 0, 1

end
