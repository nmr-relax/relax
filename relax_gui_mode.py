#! /usr/bin/env python

# This is a transient solution to forcing the GUI - a better solution would be an environmental variable.

# Import the relax module.
import relax

# Start relax.
relax.start(mode='gui')
